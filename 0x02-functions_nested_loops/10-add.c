#include <stdio.h>

/**
 * Adds two integers and prints the result.
 * 
 * @param a The first integer.
 * @param b The second integer.
 * Return: the value 
 */
int add(int a, int b)
{
	int add = a + b;

	return (add);
}

